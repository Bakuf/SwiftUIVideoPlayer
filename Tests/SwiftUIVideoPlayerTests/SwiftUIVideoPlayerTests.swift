import XCTest
@testable import SwiftUIVideoPlayer

final class SwiftUIVideoPlayerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIVideoPlayer().text, "Hello, World!")
    }
}
